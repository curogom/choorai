---
import DocsLayout from '../../../../layouts/DocsLayout.astro';
import Stepper from '../../../../components/Stepper';
import ChallengeProgressBar from '../../../../components/ChallengeProgressBar';
import Checklist from '../../../../components/Checklist';
import CodeBlock from '../../../../components/CodeBlock';
import PromptBox from '../../../../components/PromptBox';
import Callout from '../../../../components/Callout';
import SectionHeader from '../../../../components/SectionHeader';
import MetaBadge from '../../../../components/MetaBadge';

const steps = [
  { label: '사전 준비', status: 'completed' as const },
  { label: '프론트엔드', status: 'current' as const },
  { label: '백엔드', status: 'upcoming' as const },
  { label: '연결', status: 'upcoming' as const },
  { label: '배포', status: 'upcoming' as const },
  { label: '완료', status: 'upcoming' as const },
];

const checklist = [
  { id: 'vite', label: 'Vite 프로젝트 생성', description: 'npm create vite' },
  { id: 'tailwind', label: 'Tailwind CSS 설치', description: '스타일링 프레임워크' },
  { id: 'dev', label: '개발 서버 실행', description: 'npm run dev로 확인' },
  { id: 'component', label: '첫 컴포넌트 생성', description: 'AI로 UI 만들기' },
];
---

<DocsLayout
  title="60분 완주 - React 프론트엔드"
  description="React + Vite로 프론트엔드를 만들어봅시다"
  currentPath="/start/60min/frontend/react"
>
  <div class="max-w-4xl mx-auto px-6 py-8">
    <!-- 브레드크럼 -->
    <nav class="flex items-center gap-2 text-sm text-text-secondary mb-6" aria-label="현재 위치">
      <a href="/" class="hover:text-white transition-colors">홈</a>
      <span>/</span>
      <a href="/start/60min" class="hover:text-white transition-colors">60분 완주</a>
      <span>/</span>
      <span class="text-text-secondary">프론트엔드</span>
      <span>/</span>
      <span class="text-primary font-medium">React</span>
    </nav>

    <!-- 진행률 -->
    <div class="mb-8 p-4 bg-surface rounded-xl border border-border">
      <ChallengeProgressBar client:load />
    </div>

    <!-- 스텝 인디케이터 -->
    <div class="mb-12">
      <Stepper client:load steps={steps} />
    </div>

    <!-- 페이지 제목 -->
    <header class="mb-8 pb-6 border-b border-border">
      <div class="flex items-center gap-3 mb-4">
        <MetaBadge icon="clock" text="예상 소요시간: 15분" />
        <span class="px-2 py-1 rounded bg-cyan-500/20 text-cyan-400 text-xs font-bold">React</span>
      </div>
      <h1 class="text-3xl md:text-4xl font-black text-white mb-4">
        프론트엔드: React + Vite
      </h1>
      <p class="text-text-secondary text-lg leading-relaxed">
        React와 Vite를 사용해 프론트엔드를 만듭니다.
        AI의 도움을 받아 복잡한 코드도 쉽게 생성할 수 있어요.
      </p>
    </header>

    <!-- TL;DR -->
    <section class="mb-10">
      <Callout client:load variant="tip" title="TL;DR (3줄 요약)">
        <ul class="list-disc list-inside space-y-1 mt-2">
          <li><code>npm create vite@latest</code>로 프로젝트 생성</li>
          <li>Tailwind CSS 설치로 스타일링 준비</li>
          <li>AI에게 "프로젝트 목록 UI 만들어줘" 요청</li>
        </ul>
      </Callout>
    </section>

    <!-- 메인 콘텐츠 -->
    <article class="prose prose-invert max-w-none">
      <!-- 섹션 1: 프로젝트 생성 -->
      <section class="mb-10">
        <SectionHeader number={1} title="프로젝트 생성" />

        <p class="text-text-secondary mb-4">
          터미널을 열고 원하는 폴더로 이동한 뒤, 아래 명령어를 실행하세요.
        </p>

        <CodeBlock
          client:load
          code={`# 프로젝트 생성
npm create vite@latest my-admin -- --template react-ts

# 폴더로 이동
cd my-admin

# 의존성 설치
npm install`}
          filename="터미널"
        />

        <Callout client:load variant="info" title="옵션 설명">
          <ul class="list-disc list-inside space-y-1 mt-2 text-sm">
            <li><strong>my-admin</strong>: 프로젝트 이름 (원하는 이름으로 변경 가능)</li>
            <li><strong>react-ts</strong>: React + TypeScript 템플릿</li>
          </ul>
        </Callout>
      </section>

      <!-- 섹션 2: Tailwind CSS -->
      <section class="mb-10">
        <SectionHeader number={2} title="Tailwind CSS 설치" />

        <p class="text-text-secondary mb-4">
          Tailwind CSS를 설치하면 클래스만으로 예쁜 UI를 만들 수 있습니다.
        </p>

        <CodeBlock
          client:load
          code={`# Tailwind 설치
npm install -D tailwindcss postcss autoprefixer

# 설정 파일 생성
npx tailwindcss init -p`}
          filename="터미널"
        />

        <p class="text-text-secondary mt-4 mb-4">
          <code>tailwind.config.js</code> 파일을 열고 아래처럼 수정하세요:
        </p>

        <CodeBlock
          client:load
          code={`/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}`}
          filename="tailwind.config.js"
        />

        <p class="text-text-secondary mt-4 mb-4">
          <code>src/index.css</code>를 열고 맨 위에 추가하세요:
        </p>

        <CodeBlock
          client:load
          code={`@tailwind base;
@tailwind components;
@tailwind utilities;`}
          filename="src/index.css"
        />
      </section>

      <!-- 섹션 3: 개발 서버 -->
      <section class="mb-10">
        <SectionHeader number={3} title="개발 서버 실행" />

        <CodeBlock
          client:load
          code={`npm run dev`}
          filename="터미널"
        />

        <p class="text-text-secondary mt-4">
          브라우저에서 <code>http://localhost:5173</code>을 열면 Vite 기본 페이지가 보입니다.
        </p>

        <div class="mt-4 p-4 bg-green-500/10 border border-green-500/30 rounded-lg">
          <div class="flex items-center gap-2 text-green-400 font-bold mb-2">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/>
            </svg>
            성공!
          </div>
          <p class="text-green-300/80 text-sm">
            Vite 로고가 보이면 프론트엔드 기본 설정이 완료된 것입니다.
          </p>
        </div>
      </section>

      <!-- 섹션 4: AI로 UI 만들기 -->
      <section class="mb-10">
        <SectionHeader number={4} title="AI로 UI 만들기" />

        <p class="text-text-secondary mb-6">
          이제 AI에게 프로젝트 관리 UI를 만들어달라고 요청합니다.
          아래 프롬프트를 복사해서 Antigravity 또는 ChatGPT에 붙여넣으세요.
        </p>

        <PromptBox
          client:load
          prompt={`React + TypeScript + Tailwind로 프로젝트 관리 앱을 만들어줘.

요구사항:
1. src/App.tsx에 메인 레이아웃 (헤더 + 콘텐츠)
2. 프로젝트 목록을 보여주는 카드 UI
3. 새 프로젝트 생성 폼 (이름, 설명 입력)
4. 프로젝트 삭제 버튼
5. 지금은 로컬 state로 데이터 관리 (나중에 API 연결할 거야)

스타일:
- 다크 모드 배경 (bg-gray-900)
- 카드는 bg-gray-800에 hover 효과
- 파란색 계열 액센트 (blue-500)

파일 구조:
- src/App.tsx
- src/components/ProjectCard.tsx
- src/components/ProjectForm.tsx
- src/types/index.ts`}
          title="AI 프롬프트"
          description="위 프롬프트를 AI에게 전달하세요. Antigravity를 사용한다면 Cmd+L로 에이전트 패널을 열 수 있습니다."
        />

        <Callout client:load variant="warning" title="AI 응답 활용 팁">
          <ul class="list-disc list-inside space-y-1 mt-2 text-sm">
            <li>AI가 생성한 코드를 그대로 파일에 붙여넣기</li>
            <li>에러가 나면 에러 메시지를 AI에게 보여주기</li>
            <li>"이 에러 어떻게 해결해?" 라고 물어보기</li>
          </ul>
        </Callout>
      </section>

      <!-- 섹션 5: 예상 결과물 -->
      <section class="mb-10">
        <SectionHeader number={5} title="예상 결과물" />

        <p class="text-text-secondary mb-4">
          AI가 만들어준 코드를 적용하면 이런 화면이 나옵니다:
        </p>

        <div class="bg-gray-800 rounded-lg p-6 border border-gray-700">
          <div class="flex justify-between items-center mb-6">
            <h3 class="text-xl font-bold text-white">프로젝트 관리</h3>
            <div class="w-3 h-3 bg-green-500 rounded-full"></div>
          </div>

          <div class="grid md:grid-cols-2 gap-4">
            <!-- 폼 미리보기 -->
            <div class="bg-gray-900 rounded-lg p-4">
              <h4 class="text-sm font-medium text-gray-400 mb-3">새 프로젝트</h4>
              <div class="space-y-3">
                <div class="bg-gray-800 rounded px-3 py-2 text-gray-500 text-sm">프로젝트 이름</div>
                <div class="bg-gray-800 rounded px-3 py-2 text-gray-500 text-sm h-16">설명</div>
                <div class="bg-blue-600 text-white text-center py-2 rounded text-sm font-medium">생성</div>
              </div>
            </div>

            <!-- 카드 미리보기 -->
            <div class="space-y-3">
              <div class="bg-gray-900 rounded-lg p-4 border-l-4 border-blue-500">
                <div class="flex justify-between items-start">
                  <div>
                    <h4 class="font-medium text-white">내 첫 프로젝트</h4>
                    <p class="text-sm text-gray-400 mt-1">AI로 만든 웹서비스</p>
                  </div>
                  <button class="text-red-400 text-sm">삭제</button>
                </div>
              </div>
              <div class="bg-gray-900 rounded-lg p-4 border-l-4 border-blue-500 opacity-50">
                <h4 class="font-medium text-white">두 번째 프로젝트</h4>
              </div>
            </div>
          </div>
        </div>

        <p class="text-text-secondary mt-4 text-sm">
          완성된 예제 코드는
          <a href="https://github.com/choorai/examples/tree/main/b2b-admin/web" class="text-primary hover:underline">
            GitHub 저장소
          </a>에서 확인할 수 있습니다.
        </p>
      </section>

      <!-- 체크리스트 -->
      <section class="mb-10">
        <Checklist
          client:load
          storageKey="60min-frontend-react"
          title="React 프론트엔드 완료 체크리스트"
          items={checklist}
        />
      </section>

      <!-- 네비게이션 -->
      <nav class="flex items-center justify-between pt-8 border-t border-border" aria-label="페이지 이동">
        <a
          href="/start/60min"
          class="group flex items-center gap-3 px-4 py-2 rounded-lg border border-border hover:bg-surface transition-colors no-underline"
        >
          <svg class="w-5 h-5 text-text-secondary group-hover:-translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
          <div class="text-left">
            <span class="text-xs text-text-secondary">이전</span>
            <span class="block text-sm text-white font-medium">사전 준비</span>
          </div>
        </a>

        <a
          href="/start/60min/backend/fastapi"
          class="group flex items-center gap-3 px-4 py-2 rounded-lg bg-primary hover:bg-primary-hover text-background transition-colors no-underline"
        >
          <div class="text-right">
            <span class="text-xs opacity-70">다음 단계</span>
            <span class="block text-sm font-bold">백엔드 만들기</span>
          </div>
          <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </nav>
    </article>
  </div>
</DocsLayout>
