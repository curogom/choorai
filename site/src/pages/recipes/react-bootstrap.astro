---
import DocsLayout from '../../layouts/DocsLayout.astro';
import Callout from '../../components/Callout';
import CodeBlock from '../../components/CodeBlock';
import PromptBox from '../../components/PromptBox';
import Checklist from '../../components/Checklist';
import TemplateSections from '../../components/TemplateSections.astro';

const prompt = `React 프로젝트를 빠르게 시작하고 싶어.
아래 조건에 맞춰 실행 가능한 초기화 절차를 만들어줘.

[입력 정보]
- 패키지 매니저: [npm/pnpm/yarn]
- TypeScript 사용 여부: [true/false]
- 스타일링: [Tailwind/CSS Modules]
- 상태관리: [zustand/redux/none]
- 배포 대상: [Cloudflare Pages/Vercel]

[출력 요구사항]
1) 프로젝트 생성 명령어
2) 폴더 구조(feature-based)
3) ESLint/Prettier 설정
4) 라우팅/상태관리 기본 템플릿
5) 첫 배포 전 체크리스트`;

const checklist = [
  { id: 'create', label: '프로젝트 생성', description: 'Vite + React + TS 초기화' },
  { id: 'lint', label: '코드 품질 도구 설정', description: 'ESLint/Prettier 적용' },
  { id: 'structure', label: '폴더 구조 확정', description: 'feature-based 구조 반영' },
  { id: 'runtime', label: '실행/빌드 확인', description: 'npm run dev/build 검증' },
  { id: 'deploy-ready', label: '배포 준비 점검', description: '환경변수/SPA 라우팅 확인' },
];
---

<DocsLayout
  title="레시피: React 프로젝트 부트스트랩"
  description="Vite 기반 React 프로젝트를 빠르게 시작하는 프롬프트 레시피"
  currentPath="/recipes/react-bootstrap"
>
  <div class="max-w-4xl mx-auto px-6 py-8">
    <nav class="flex items-center gap-2 text-sm text-text-secondary mb-6" aria-label="현재 위치">
      <a href="/" class="hover:text-white transition-colors">홈</a>
      <span>/</span>
      <a href="/recipes" class="hover:text-white transition-colors">Recipes</a>
      <span>/</span>
      <span class="text-primary font-medium">React 부트스트랩</span>
    </nav>

    <header class="mb-8 pb-6 border-b border-border">
      <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-purple-500/10 text-purple-300 text-xs font-bold mb-4">
        Recipes Core Pack · Frontend
      </div>
      <h1 class="text-3xl md:text-4xl font-black text-white mb-4">React 프로젝트 부트스트랩</h1>
      <p class="text-text-secondary text-lg leading-relaxed">
        신규 React 프로젝트를 생성하고 기본 품질 도구/구조까지 한 번에 잡는 레시피입니다.
      </p>
    </header>

    <section class="mb-8">
      <Callout client:load variant="tip" title="추천 사용 시점">
        <p class="mt-2">새 프로젝트 킥오프, 리포지토리 재정비, 팀 공통 템플릿 정리 시점에 사용하세요.</p>
      </Callout>
    </section>

    <section class="mb-10">
      <PromptBox client:load prompt={prompt} title="React 부트스트랩 프롬프트" />
    </section>

    <section class="mb-10">
      <h2 class="text-2xl font-bold text-white mb-4">명령어 예시</h2>
      <CodeBlock
        client:load
        filename="terminal"
        code={`npm create vite@latest my-app -- --template react-ts
cd my-app
npm install
npm install -D eslint prettier
npm run dev`}
      />
    </section>

    <section class="mb-10">
      <Checklist client:load storageKey="recipe-react-bootstrap" title="실행 체크리스트" items={checklist} />
    </section>

    <TemplateSections topic="react-bootstrap" />

    <section class="mt-12 pt-8 border-t border-border">
      <h3 class="text-lg font-bold text-white mb-4">관련 문서</h3>
      <div class="grid md:grid-cols-2 gap-4">
        <a href="/map/frontend/react" class="p-4 bg-surface border border-border rounded-lg hover:border-primary/50 transition-colors no-underline">
          <h4 class="text-white font-medium mb-1">React 학습 경로</h4>
          <p class="text-text-secondary text-sm">컴포넌트/상태관리 기본기</p>
        </a>
        <a href="/fix/build-fail" class="p-4 bg-surface border border-border rounded-lg hover:border-primary/50 transition-colors no-underline">
          <h4 class="text-white font-medium mb-1">빌드 실패 해결</h4>
          <p class="text-text-secondary text-sm">초기 세팅 오류 빠른 점검</p>
        </a>
      </div>
    </section>
  </div>
</DocsLayout>
